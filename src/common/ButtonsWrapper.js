import styled from '@emotion/styled';

const ButtonsWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  padding: 0.5rem 1rem;

  > button {
    flex: 1 1 100%;
    margin: 0.25rem 0;
  }
`;

export default ButtonsWrapper;
